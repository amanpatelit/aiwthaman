version: '3.8'

services:
  web:
    build: .
    container_name: amanpatel-portfolio
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
